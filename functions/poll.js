exports.handler = async function (event, context) {
  // Check pin
  // Check id_capture

  // Proxy request
  
  return {
    statusCode: 200,
    body: JSON.stringify({ message: "Hello World"}),
  }
}